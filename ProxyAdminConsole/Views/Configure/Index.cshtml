@model IEnumerable<ProxyAdminConsole.Models.ConfigModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RuleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pattern)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RuleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pattern)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.RuleName/* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Detail", new { id = item.RuleName/* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RuleName/* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
